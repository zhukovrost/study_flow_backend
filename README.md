# StudyFlow - Task-tracker для студентов

StudyFlow - это веб-приложение для трекинга учебных целей, которое помогает студентам повысить эффективность планирования учебного процесса. Приложение учитывает энергетическое состояние пользователя и предоставляет персонализированные рекомендации.

## Архитектура

📊 **Схема базы данных и архитектуры приложения**: [Смотреть на Google Drive](https://drive.google.com/file/d/1i9VX0E8YTMDkGtvh4-ObjBdf9cHQLj07/view?usp=sharing)

## Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **SQLite** - база данных
- **SQLAlchemy** - ORM для работы с БД
- **Pydantic** - валидация данных
- **JWT** - аутентификация через JSON Web Tokens

## Структура проекта

```
studyflow/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа в приложение
│   ├── deps.py              # Зависимости FastAPI
│   ├── core/
│   │   ├── config.py        # Конфигурация приложения
│   │   └── security.py      # JWT и безопасность
│   ├── database/
│   │   └── base.py          # Настройка БД
│   ├── models/
│   │   ├── user.py          # Модель пользователя
│   │   ├── task.py          # Модель задачи
│   │   └── category.py      # Модель категории
│   ├── schemas/
│   │   ├── user.py          # Pydantic схемы для пользователя
│   │   ├── task.py          # Pydantic схемы для задачи
│   │   └── category.py      # Pydantic схемы для категории
│   ├── crud/
│   │   ├── user.py          # CRUD операции для пользователя
│   │   ├── task.py          # CRUD операции для задачи
│   │   └── category.py      # CRUD операции для категории
│   └── api/
│       ├── auth.py          # API для аутентификации
│       ├── tasks.py         # API для задач
│       └── categories.py    # API для категорий
├── requirements.txt
├── .env                      # Переменные окружения
├── .gitignore
├── README.md
└── QUICKSTART.md
```

## Установка

1. Клонируйте репозиторий или создайте директорию проекта
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Запустите приложение:

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: `http://localhost:8000`

## API Документация

После запуска приложения доступна интерактивная документация Swagger UI:
- Swagger UI: `http://localhost:8000/docs`

## Использование API

### Регистрация пользователя

```bash
POST /api/v1/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "username": "username",
  "password": "password123",
  "name": "John",
  "surname": "Doe"
}
```

### Вход (получение JWT токена)

```bash
POST /api/v1/auth/login
Content-Type: application/json

{
  "username": "username",
  "password": "password123"
}
```

Ответ:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "bearer"
}
```

### Создание задачи

```bash
POST /api/v1/tasks/
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "title": "Изучить FastAPI",
  "description": "Изучить основы FastAPI",
  "priority": 2,
  "category_id": 1,
  "scheduled_date": "2024-01-10T09:00:00",
  "due_date": "2024-01-15T23:59:59"
}
```

### Получение списка задач

```bash
GET /api/v1/tasks/
Authorization: Bearer <your_token>

# С фильтром по выполненным задачам
GET /api/v1/tasks/?is_completed=false
```

### Отметить задачу как выполненную

```bash
POST /api/v1/tasks/{task_id}/complete
Authorization: Bearer <your_token>
```

### Обновить задачу

```bash
PUT /api/v1/tasks/{task_id}
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "title": "Обновленное название",
  "is_completed": true
}
```

### Удалить задачу

```bash
DELETE /api/v1/tasks/{task_id}
Authorization: Bearer <your_token>
```

## Разработка

Для разработки рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

## Конфигурация

Переменные окружения настраиваются через файл `.env`:
- `SECRET_KEY` - секретный ключ для JWT
- `DATABASE_URL` - URL базы данных
- `ACCESS_TOKEN_EXPIRE_MINUTES` - время жизни токена

## База данных

При первом запуске автоматически создается база данных SQLite в файле `studyflow.db` со всеми необходимыми таблицами.

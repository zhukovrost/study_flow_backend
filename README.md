# StudyFlow - Task-tracker для студентов

StudyFlow - это веб-приложение для трекинга учебных целей, которое помогает студентам повысить эффективность планирования учебного процесса. Приложение учитывает энергетическое состояние пользователя и предоставляет персонализированные рекомендации.

## Архитектура

📊 **Схема базы данных и архитектуры приложения**: [Смотреть на Google Drive](https://drive.google.com/file/d/1i9VX0E8YTMDkGtvh4-ObjBdf9cHQLj07/view?usp=sharing)

## Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **SQLite** - база данных
- **SQLAlchemy** - ORM для работы с БД
- **Pydantic** - валидация данных
- **JWT** - аутентификация через JSON Web Tokens

## Структура проекта (основные файлы)

```
studyflow/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа в приложение (FastAPI app)
│   ├── deps.py              # Зависимости FastAPI
│   ├── core/                # config, security
│   ├── database/            # init DB, Base
│   ├── models/              # SQLAlchemy модели (User, Task, TaskList и т.д.)
│   ├── schemas/             # Pydantic схемы
│   ├── crud/                # CRUD операции
│   └── api/                 # Роутеры (auth, tasks, tasklists, analytics)
├── requirements.txt
├── .env                      # Переменные окружения
├── .gitignore
├── README.md
└── QUICKSTART.md
```

## Установка

1. Клонируйте репозиторий или создайте директорию проекта
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Запустите приложение:

```bash
uvicorn app.main:app --reload
```

Приложение будет доступно по адресу: `http://localhost:8000`

## API Документация

После запуска приложения доступна интерактивная документация Swagger UI:
- Swagger UI: `http://localhost:8000/docs`

Обратите внимание на ключевые группы эндпоинтов:

- Authentication: `/api/v1/auth/*`
- Tasks: `/api/v1/tasks/*`
- TaskLists (списки задач): `/api/v1/tasklists/*`

TaskLists теперь доступны только их создателю (поведение аналогично Google Tasks).

Подзадачи: модель Task поддерживает `parent_id` — вы можете создавать подзадачи, передавая `parent_id` в теле создания/обновления задачи. Для этого в БД должно быть поле `parent_id` (см. `migrations/001_add_parent_to_tasks.sql`).

## Использование API

### Регистрация пользователя

```bash
POST /api/v1/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "username": "username",
  "password": "password123",
  "name": "John",
  "surname": "Doe"
}
```

### Вход (получение JWT токена)

```bash
POST /api/v1/auth/login
Content-Type: application/json

{
  "username": "username",
  "password": "password123"
}
```

Ответ:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "bearer"
}
```

### Создание задачи

```bash
POST /api/v1/tasks/
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "title": "Изучить FastAPI",
  "description": "Изучить основы FastAPI",
  "priority": 2,
  "category_id": 1,
  "scheduled_date": "2024-01-10T09:00:00",
  "due_date": "2024-01-15T23:59:59"
}
```

### Получение списка задач

```bash
GET /api/v1/tasks/
Authorization: Bearer <your_token>

# С фильтром по выполненным задачам
GET /api/v1/tasks/?is_completed=false
```

### Отметить задачу как выполненную

```bash
POST /api/v1/tasks/{task_id}/complete
Authorization: Bearer <your_token>
```

### Обновить задачу

```bash
PUT /api/v1/tasks/{task_id}
Authorization: Bearer <your_token>
Content-Type: application/json

{
  "title": "Обновленное название",
  "is_completed": true
}
```

### Удалить задачу

```bash
DELETE /api/v1/tasks/{task_id}
Authorization: Bearer <your_token>
```

### Подзадачи

Чтобы создать подзадачу, в теле создания задачи укажите `parent_id` с id родительской задачи, принадлежащей тому же пользователю:

```bash
POST /api/v1/tasks/
{
  "title": "Подзадача",
  "parent_id": 123
}
```

Для корректной работы убедитесь, что выполнена миграция, добавляющая колонку `parent_id` в таблицу `tasks` (`migrations/001_add_parent_to_tasks.sql`).

## Разработка

Для разработки рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

## Конфигурация

Переменные окружения настраиваются через файл `.env`:
- `SECRET_KEY` - секретный ключ для JWT
- `DATABASE_URL` - URL базы данных
- `ACCESS_TOKEN_EXPIRE_MINUTES` - время жизни токена
